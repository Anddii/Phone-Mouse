### Infrastructure:

Node.js as a web server hosting a react app. React app call's Node server and sends commands.

Server is using [RobotJS](http://robotjs.io/)

### Features:
- Moving a mouse
- Scrolling Y
- Keyboard
- Left and Right Click  

### ToDo:
- Mouse Hold
- Settings
    - Sensitivy
- Zoom
- Volume

## Build 

### Building a server

``` tsc _server.ts ```

If you wan't to build a executable use pkg

### Building a client

``` yarn ```

``` yarn build ```

or 

``` npm i ```

``` npm run build ```

